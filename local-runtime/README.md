# Local runtime boundary

Host-specific state, credentials, caches, logs, and working memory stay outside
the public repository. Add local configuration through ignored files or a
separate private deployment layer.
