# Contributing

Keep the runtime host-neutral and Bun-compatible. Changes to routing, provider
normalization, tools, or permissions must include a focused test. Do not add
credentials, personal data, machine-specific paths, or private workspace
snapshots. Run `npm run check` before opening a pull request.
